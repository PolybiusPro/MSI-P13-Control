# EVDI is a connectorless virtual DRM device used after normal boot.  Keeping
# it out of the initramfs prevents Plymouth from selecting it before the
# physical GPU is available.
omit_drivers+=" evdi "
